{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local palette editing and packaging.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
